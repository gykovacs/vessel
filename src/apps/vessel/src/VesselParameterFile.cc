#include <VesselParameterFile.h>

/*VesselParameterFile::VesselParameterFile(std::string filename)
: ParameterFile(filename)
{
    add("roiradius", PARAMETER_FLOAT, 1);
    add("nw", PARAMETER_FLOAT, 1);
    add("tdyn", PARAMETER_FLOAT, 1);
    add("tlength", PARAMETER_FLOAT, 1);
    add("tthin", PARAMETER_FLOAT, 1);
    add("thinwavelength", PARAMETER_FLOAT, 1);
    
    next("roiradius");
    roiradius= tokenFloat;
    next("nw");
    nw= tokenFloat;
    next("tdyn");
    tdyn= tokenFloat;
    next("tlength");
    tlength= tokenFloat;
    next("tthin");
    tthin= tokenFloat;
    next("thinwavelength");
    thinwavelength= tokenFloat;
}

VesselParameterFile::VesselParameterFile(const VesselParameterFile& d)
: ParameterFile(d)
{
}

VesselParameterFile::~VesselParameterFile()
{
}*/